version: '3'

tasks:
  delete-kind-cluster:
    summary: |
      Delete the KinD cluster.
    cmds:
      - kind delete cluster --name capt-playground
    status:
      - kind get clusters | grep -v capt-playground