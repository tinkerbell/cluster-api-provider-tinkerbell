# arm64 drop-in: EFI-only boot.
# arm64 servers boot via UEFI; no BIOS boot partition is needed.

[Match]
Architecture=arm64

[Content]
Packages=
        grub-efi-arm64-bin
        grub-efi-arm64-signed
        shim-signed
